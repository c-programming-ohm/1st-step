#include <stdio.h>

int main(void)
{
    int a, b, c, d, e, f, g;

    a = 10;
    b = 3;
    c = a + b;
    d = a - b;
    e = a * b;
    f = a / b;
    g = a % b;
    printf("Answer : %d,%d,%d,%d,%d\n", c, d, e, f, g);

    return 0;
}
